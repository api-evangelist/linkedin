naftiko: 1.0.0-alpha2
info:
  label: LinkedIn Parent Application Management API — Application Updates
  description: 'LinkedIn Parent Application Management API — Application Updates. 1 operations. Lead operation: LinkedIn Update
    OAuth 2.0 Callback URLs for Child Application. Self-contained Naftiko capability covering one Linkedin business surface.'
  tags:
  - Linkedin
  - Application Updates
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    LINKEDIN_API_KEY: LINKEDIN_API_KEY
capability:
  consumes:
  - type: http
    namespace: talent-learning-parent-application-application-updates
    baseUri: https://api.linkedin.com
    description: LinkedIn Parent Application Management API — Application Updates business capability. Self-contained, no
      shared references.
    resources:
    - name: v2-provisionedApplications-customer_application_urn
      path: /v2/provisionedApplications/{customer_application_urn}
      operations:
      - name: updatechildapplicationcallbackurls
        method: POST
        description: LinkedIn Update OAuth 2.0 Callback URLs for Child Application
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.LINKEDIN_API_KEY}}'
  exposes:
  - type: rest
    namespace: talent-learning-parent-application-application-updates-rest
    port: 8080
    description: REST adapter for LinkedIn Parent Application Management API — Application Updates. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/v2/provisionedapplications/{customer-application-urn}
      name: v2-provisionedapplications-customer-application-urn
      description: REST surface for v2-provisionedApplications-customer_application_urn.
      operations:
      - method: POST
        name: updatechildapplicationcallbackurls
        description: LinkedIn Update OAuth 2.0 Callback URLs for Child Application
        call: talent-learning-parent-application-application-updates.updatechildapplicationcallbackurls
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: talent-learning-parent-application-application-updates-mcp
    port: 9090
    transport: http
    description: MCP adapter for LinkedIn Parent Application Management API — Application Updates. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: linkedin-update-oauth-2-0-callback
      description: LinkedIn Update OAuth 2.0 Callback URLs for Child Application
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: talent-learning-parent-application-application-updates.updatechildapplicationcallbackurls
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
